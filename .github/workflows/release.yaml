name: Release
on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/release.yaml@8f09bcd5f8ac39bc5059f4c45d3fcb10a1a3b23a # v1
    with:
      target: go
