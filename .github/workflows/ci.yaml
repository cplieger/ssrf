name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: cplieger/ci/.github/workflows/go-ci.yaml@8f09bcd5f8ac39bc5059f4c45d3fcb10a1a3b23a # v1
